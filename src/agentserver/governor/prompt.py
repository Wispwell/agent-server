"""Governor prompt construction. M3.

State digest is injected here by the supervisor each turn — the governor never
fetches it.

TODO(M3): system prompt, digest rendering, output format instruction
"""
