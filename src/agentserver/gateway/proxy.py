"""Tool call proxy. M2.

    verify PoP -> ask admission -> inject real credentials -> call real MCP
    server -> report ET consumed

TODO(M2): request handling, ET verification, consumption reporting
"""
