"""Ledger event types. M0.

Every decision is recorded — APPROVED, ESCALATED and DENIED alike, not just
successes — plus token issue/revoke/consume and lifecycle transitions.

TODO(M0): event schema, event type enum
"""
