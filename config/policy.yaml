# Containment policy. Deterministic, explainable, no tuning knobs pretending to
# be science. Deliberately NOT a risk-score formula.

budgets: {}
  # max_concurrent_subagents: 3
  # max_spawns_per_task: 10
  # max_wall_clock_seconds: 600
  # max_tool_calls_per_subagent: 50

admission: {}
  # cooldown:
  #   denials: 3
  #   window_seconds: 600
  #   lock_seconds: 300
  # rate_limit_per_pattern_key:
  #   count: 20
  #   window_seconds: 60

delegation: {}
  # max_depth: 1   # supervisor -> subagent only in v0
